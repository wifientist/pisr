# PISR runtime dependencies.
#
# Versions match rtools2's, so this inherits a combination already proven in
# production. WeasyPrint's own transitives (pydyf, tinycss2, cssselect2, Pyphen,
# fontTools, Pillow, tinyhtml5) are deliberately unpinned — let pip resolve what
# 68.1 actually wants, then lock the result:
#
#   docker compose run --rm pisr pip freeze > api/requirements.lock.txt
#
# Dropped from rtools2 (23 packages): SQLAlchemy, alembic, psycopg2-binary,
# redis, APScheduler, boto3, cryptography, python-jose, python-multipart,
# httpx, aiohttp, playwright, PyYAML, Mako, email_validator, dnspython, ecdsa,
# rsa, pyasn1, six, greenlet, exceptiongroup, starkbank-ecdsa.
#
# cryptography goes with the database: credentials are plaintext in .env now,
# which is the right trade for a single-purpose tool but worth knowing.

# ── Web framework ──
fastapi==0.115.11
starlette==0.46.1
uvicorn==0.34.0
h11==0.14.0
anyio==4.8.0
sniffio==1.3.1
click==8.1.8

# ── Validation (FastAPI's own) ──
pydantic==2.10.6
pydantic_core==2.27.2
annotated-types==0.7.0
typing_extensions==4.12.2

# ── Configuration ──
python-dotenv==1.0.1

# ── RUCKUS ONE client (r1api/ is requests-based) ──
requests==2.32.3
certifi==2025.4.26
charset-normalizer==3.4.1
idna==3.10
urllib3==2.4.0

# ── Cloudflare Access JWT verification (optional; PISR_ACCESS_TEAM) ──
#
# cryptography was dropped when PISR was extracted — see the note above, it
# went with the database. It comes back for one reason: verifying an RS256
# assertion is the difference between proxy mode resting on proof and proxy
# mode resting on the network boundary alone. Wheels exist for python:3.11-slim
# so nothing is compiled at build time.
#
# Imported only when PISR_ACCESS_TEAM and PISR_ACCESS_AUD are both set. An
# oauth2-proxy deployment never loads either package.
PyJWT==2.10.1
cryptography==44.0.2

# ── PDF report ──
Jinja2==3.1.4
MarkupSafe==3.0.2
weasyprint==68.1
cffi==1.17.1
pycparser==2.22
