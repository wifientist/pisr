# Credentials. This is the whole secret store — never commit it.
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node
node_modules/
dist/
.vite/

# Generated
api/requirements.lock.txt
*.pdf
*.log

# Editors / OS
.vscode/
.idea/
.DS_Store

# Other env files people end up with. .env.example is the one exception —
# it holds no values, only the shape and the reasoning.
.env.*
!.env.example

# Cookie jars from the curl recipes in the README
jar

# The RUCKUS ONE OpenAPI document. Not committed: it is a 7MB vendor artefact
# that would sit in history forever, and it is regenerated by RUCKUS rather
# than maintained here. Drop it in spec/ yourself — CLAUDE.md explains what it
# is for, and everything still works without it.
spec/
